# 📱 실제 앱 확인하기 - 단계별 가이드

## 🎯 가장 빠른 방법 (3단계)

### 1단계: 필수 프로그램 설치 확인

```bash
# D:\아이돌\idol-filter-app 폴더에서
node check-env.js
```

이 스크립트가 다음을 확인합니다:
- ✅ Node.js 설치 여부
- ✅ Java 설치 여부  
- ✅ Android SDK 설정
- ✅ 연결된 기기/에뮬레이터

---

## 📲 방법 1: Android 에뮬레이터 (추천)

### 준비사항
1. **Android Studio 설치** (없는 경우)
   - https://developer.android.com/studio 다운로드
   - 설치 시 "Android SDK", "Android Emulator" 체크

2. **에뮬레이터 생성**
   - Android Studio 실행
   - **Tools → Device Manager**
   - **Create Device** 클릭
   - Pixel 5 또는 원하는 기기 선택
   - 시스템 이미지 다운로드 (API 33 이상 권장)
   - **Finish** 클릭

### 실행 단계

```bash
# 1. 프로젝트 폴더로 이동
cd D:\아이돌\idol-filter-app

# 2. 환경 체크
node check-env.js

# 3. 의존성 설치 (처음 한 번만)
npm install

# 4. Android 프로젝트 초기화 (android 폴더가 없는 경우)
# 이 단계는 React Native CLI로 android 폴더를 생성합니다
npx @react-native-community/cli init TempProject --skip-install
# TempProject/android 폴더를 idol-filter-app/로 복사
# 또는 아래 명령으로 직접 생성:
npx react-native init IdolFilterApp --template react-native-template-typescript
# 생성된 android 폴더를 idol-filter-app/로 복사

# 5. 에뮬레이터 실행
# Android Studio → Device Manager → 에뮬레이터 ▶️ 버튼 클릭
# 또는 명령어로:
emulator -avd [에뮬레이터이름]

# 6. Metro 번들러 시작 (터미널 1)
npm start

# 7. 앱 실행 (터미널 2 - 새 터미널 열기)
npm run android
```

**또는 배치 파일 사용:**
1. `빠른시작.bat` 더블클릭 (Metro 번들러 시작)
2. 새 터미널에서 `Android실행.bat` 더블클릭

---

## 📱 방법 2: 실제 Android 기기 (USB 연결)

### 준비사항
1. **기기에서 개발자 옵션 활성화**
   - 설정 → 휴대전화 정보 → 빌드 번호 7번 연속 탭
   
2. **USB 디버깅 활성화**
   - 설정 → 개발자 옵션 → USB 디버깅 ON

3. **USB 케이블로 PC 연결**
   - 기기에서 "USB 디버깅 허용" 팝업 확인

### 실행 단계

```bash
# 1. 기기 연결 확인
adb devices
# 연결된 기기가 보여야 함

# 2. 프로젝트 폴더로 이동
cd D:\아이돌\idol-filter-app

# 3. 의존성 설치
npm install

# 4. Metro 번들러 시작
npm start

# 5. 새 터미널에서 앱 실행
npm run android
```

---

## 🚨 문제 해결

### 문제 1: "android 폴더가 없습니다"

**해결책:**
React Native 프로젝트를 초기화해야 합니다:

```bash
# 방법 A: 임시 프로젝트 생성 후 복사
npx react-native init TempProject
# TempProject/android 폴더를 D:\아이돌\idol-filter-app\로 복사

# 방법 B: 직접 생성 (권장)
cd D:\아이돌\idol-filter-app
npx @react-native-community/cli init . --skip-install
# 또는
npx react-native init IdolFilterApp --template react-native-template-typescript
# 생성된 android 폴더를 현재 폴더로 복사
```

### 문제 2: "SDK location not found"

**해결책:**
프로젝트 루트에 `local.properties` 파일 생성:

```properties
sdk.dir=C:\\Users\\YourUsername\\AppData\\Local\\Android\\Sdk
```

실제 경로는 Android Studio → SDK Manager에서 확인 가능

### 문제 3: "포트 8081이 이미 사용 중"

**해결책:**
```bash
# Windows
netstat -ano | findstr :8081
taskkill /PID [프로세스ID] /F

# 또는 캐시 삭제
npm start -- --reset-cache
```

### 문제 4: "Metro bundler 오류"

**해결책:**
```bash
# 캐시 삭제 후 재시작
npm start -- --reset-cache
```

### 문제 5: "Gradle 빌드 실패"

**해결책:**
```bash
cd android
./gradlew clean
cd ..
npm run android
```

---

## ✅ 실행 확인 체크리스트

실행 전 확인:

- [ ] Node.js 18 이상 설치됨
- [ ] Java JDK 17 이상 설치됨
- [ ] Android Studio 설치됨
- [ ] Android SDK 설치됨 (API 33+)
- [ ] 환경변수 ANDROID_HOME 설정됨
- [ ] 에뮬레이터 실행 중 또는 실제 기기 연결됨
- [ ] `npm install` 완료됨
- [ ] `android/` 폴더 존재함
- [ ] `local.properties` 파일 생성됨

---

## 🎬 실제 실행 순서 (요약)

### 첫 실행 (한 번만)

```bash
cd D:\아이돌\idol-filter-app

# 1. 환경 체크
node check-env.js

# 2. 의존성 설치
npm install

# 3. Android 프로젝트 초기화 (android 폴더가 없는 경우)
# (위의 "문제 해결 1" 참고)

# 4. local.properties 파일 생성
# (위의 "문제 해결 2" 참고)
```

### 매번 실행

```bash
# 터미널 1: Metro 번들러
cd D:\아이돌\idol-filter-app
npm start

# 터미널 2: 앱 실행
cd D:\아이돌\idol-filter-app
npm run android
```

---

## 💡 팁

1. **첫 실행은 시간이 오래 걸립니다** (Gradle 빌드)
2. **에뮬레이터는 미리 실행**해두는 것이 좋습니다
3. **에러가 나면** `npm start -- --reset-cache` 시도
4. **Android Studio의 Logcat**에서 상세 로그 확인 가능

---

## 📞 추가 도움

문제가 계속되면:
1. `실행가이드.md` 파일 참고
2. Android Studio의 Logcat에서 에러 메시지 확인
3. `node check-env.js`로 환경 재확인

