# Idol Filter App 실행 가이드

## 📱 앱을 확인하는 방법

### 방법 1: Android 에뮬레이터 (Windows 권장)

#### 1단계: 필수 프로그램 설치

1. **Node.js 설치** (이미 설치되어 있을 수 있음)
   - https://nodejs.org 에서 LTS 버전 다운로드
   - 설치 후 터미널에서 확인:
   ```bash
   node --version  # v18 이상 필요
   npm --version
   ```

2. **Java JDK 설치**
   - https://adoptium.net 에서 JDK 17 다운로드
   - 환경변수 설정 필요

3. **Android Studio 설치**
   - https://developer.android.com/studio
   - 설치 시 "Android SDK", "Android SDK Platform", "Android Virtual Device" 체크

#### 2단계: Android Studio 설정

1. Android Studio 실행
2. **Tools → SDK Manager** 열기
3. **SDK Platforms** 탭에서:
   - Android 13.0 (Tiramisu) 또는 최신 버전 체크
4. **SDK Tools** 탭에서:
   - Android SDK Build-Tools
   - Android Emulator
   - Android SDK Platform-Tools
   체크 후 설치

#### 3단계: 환경변수 설정

Windows 환경변수에 추가:
```
ANDROID_HOME = C:\Users\사용자명\AppData\Local\Android\Sdk
```

Path에 추가:
```
%ANDROID_HOME%\platform-tools
%ANDROID_HOME%\emulator
%ANDROID_HOME%\tools
%ANDROID_HOME%\tools\bin
```

#### 4단계: Android 에뮬레이터 생성

1. Android Studio → **Tools → Device Manager**
2. **Create Device** 클릭
3. 원하는 기기 선택 (예: Pixel 5)
4. 시스템 이미지 선택 (API 33 이상 권장)
5. **Finish** 클릭

#### 5단계: React Native 프로젝트 초기화

```bash
# 프로젝트 폴더로 이동
cd idol-filter-app

# 의존성 설치
npm install

# React Native CLI 전역 설치 (없는 경우)
npm install -g react-native-cli

# Android 프로젝트 초기화
npx react-native init IdolFilterApp --template react-native-template-typescript
# 주의: 이 명령은 새 프로젝트를 만듭니다. 
# 대신 아래 방법 사용:
```

**더 쉬운 방법:**
```bash
# 프로젝트 폴더에서
npx @react-native-community/cli init IdolFilterApp --skip-install
# 또는 기존 프로젝트에 android/ios 폴더 추가
```

#### 6단계: Android 에뮬레이터 실행

1. Android Studio → **Tools → Device Manager**
2. 만든 에뮬레이터 옆 **▶️ Play** 버튼 클릭
3. 에뮬레이터가 완전히 부팅될 때까지 대기

#### 7단계: 앱 실행

```bash
# 프로젝트 폴더에서
cd idol-filter-app

# Metro 번들러 시작 (새 터미널)
npm start

# 다른 터미널에서 Android 실행
npm run android
```

---

### 방법 2: 실제 Android 기기 (USB 연결)

#### 1단계: 개발자 옵션 활성화

1. 기기에서 **설정 → 휴대전화 정보**
2. **빌드 번호**를 7번 연속 탭
3. **개발자 옵션** 활성화

#### 2단계: USB 디버깅 활성화

1. **설정 → 개발자 옵션**
2. **USB 디버깅** 켜기

#### 3단계: 기기 연결

1. USB 케이블로 PC에 연결
2. 기기에서 "USB 디버깅 허용" 확인

#### 4단계: 기기 확인

```bash
adb devices
# 연결된 기기가 보여야 함
```

#### 5단계: 앱 실행

```bash
npm run android
```

---

### 방법 3: Expo Go (가장 쉬운 방법, 제한 있음)

**주의**: 현재 프로젝트는 네이티브 모듈(react-native-vision-camera)을 사용하므로 Expo Go에서는 일부 기능이 작동하지 않을 수 있습니다.

```bash
# Expo CLI 설치
npm install -g expo-cli

# 프로젝트에서
npx expo start

# QR 코드 스캔하여 Expo Go 앱으로 실행
```

---

## 🚨 문제 해결

### 문제 1: "android 폴더가 없습니다"

**해결책:**
```bash
# React Native 프로젝트 초기화
npx react-native init TempProject
# TempProject/android 폴더를 idol-filter-app/로 복사
# TempProject/ios 폴더를 idol-filter-app/로 복사 (Mac만)
```

### 문제 2: "SDK location not found"

**해결책:**
프로젝트 루트에 `local.properties` 파일 생성:
```properties
sdk.dir=C:\\Users\\사용자명\\AppData\\Local\\Android\\Sdk
```

### 문제 3: "Metro bundler 오류"

**해결책:**
```bash
# 캐시 삭제
npm start -- --reset-cache

# 또는
npx react-native start --reset-cache
```

### 문제 4: "포트 8081이 이미 사용 중"

**해결책:**
```bash
# Windows에서 포트 사용 중인 프로세스 종료
netstat -ano | findstr :8081
taskkill /PID [프로세스ID] /F
```

---

## 📋 빠른 체크리스트

실행 전 확인사항:

- [ ] Node.js 설치됨 (v18+)
- [ ] Java JDK 설치됨 (v17+)
- [ ] Android Studio 설치됨
- [ ] Android SDK 설치됨
- [ ] 환경변수 설정됨
- [ ] 에뮬레이터 또는 실제 기기 연결됨
- [ ] `npm install` 완료됨
- [ ] `android/` 폴더 존재함

---

## 🎯 가장 빠른 시작 방법 (권장)

1. **Android Studio 설치** (위 참고)
2. **에뮬레이터 생성 및 실행**
3. **프로젝트 폴더에서:**
   ```bash
   npm install
   npm start
   # 새 터미널에서
   npm run android
   ```

---

## 💡 팁

- 첫 실행은 시간이 오래 걸릴 수 있습니다 (Gradle 빌드)
- 에러가 나면 `npm start -- --reset-cache` 시도
- Android Studio의 Logcat에서 상세 로그 확인 가능

