# GlowCam 배경화면 설치 가이드

화려한 애니메이션 배경화면이 추가되었습니다! 🎨✨

## 설치 방법

### 1. 패키지 설치

터미널에서 다음 명령어를 실행하세요:

```bash
cd D:\GlowCam\idol-filter-app
npm install react-native-linear-gradient
```

### 2. Android 네이티브 설정

#### Android Studio에서:

1. **File → Sync Project with Gradle Files** 클릭
2. 또는 터미널에서:
   ```bash
   cd android
   ./gradlew clean
   ```

### 3. iOS 설정 (Mac만 해당)

```bash
cd ios
pod install
```

## 추가된 기능

### 1. **AnimatedBackground** 컴포넌트
- 화려한 그라데이션 배경
- 애니메이션 글로우 효과
- 반짝이는 스파클 효과
- 펄스 애니메이션

### 2. **SplashScreen** 컴포넌트
- 앱 시작 시 스플래시 화면
- 로고 애니메이션
- 로딩 인디케이터

### 3. **홈 화면 배경**
- 자동으로 화려한 배경 적용
- 그라데이션 + 글로우 효과

## 사용 방법

배경화면은 자동으로 적용됩니다:

- **홈 화면**: `AnimatedBackground` 자동 적용
- **스플래시**: 앱 시작 시 자동 표시
- **카메라 화면**: 필요시 추가 가능

## 커스터마이징

### 배경 색상 변경

`src/components/AnimatedBackground.tsx` 파일에서:

```typescript
const getGradientColors = () => {
  switch (variant) {
    case 'home':
      return [
        ['#000000', '#1a0033', '#330066'],  // 배경 그라데이션
        ['#FF7CCB', '#FFB6E1', '#FFD6F0'],  // 글로우 1
        ['#667eea', '#764ba2', '#f093fb'],  // 글로우 2
      ];
    // ...
  }
};
```

### 애니메이션 속도 조절

`AnimatedBackground.tsx`의 `useEffect`에서 duration 값 변경:

```typescript
Animated.timing(glowAnim1, {
  toValue: 1,
  duration: 3000,  // 이 값을 변경 (밀리초)
  useNativeDriver: true,
}),
```

## 문제 해결

### 1. "react-native-linear-gradient" 모듈을 찾을 수 없음

```bash
npm install react-native-linear-gradient
cd android
./gradlew clean
```

### 2. 빌드 오류

```bash
cd android
./gradlew clean
cd ..
npm start -- --reset-cache
```

### 3. 애니메이션이 작동하지 않음

- Metro bundler 재시작: `npm start -- --reset-cache`
- 앱 재빌드: `npm run android`

## 효과 미리보기

배경화면에는 다음 효과가 포함됩니다:

- ✨ **그라데이션 배경**: 부드러운 색상 전환
- 💫 **글로우 효과**: 펄스하는 빛 효과
- ⭐ **스파클**: 반짝이는 별 효과
- 🌈 **다중 레이어**: 깊이감 있는 배경

모든 효과는 성능 최적화를 위해 `useNativeDriver: true`를 사용합니다!

