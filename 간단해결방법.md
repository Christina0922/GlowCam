# 🎯 android 폴더 생성 - 간단 해결 방법

React Native CLI에 문제가 있을 때 가장 쉬운 방법입니다.

## ✅ 가장 쉬운 방법: Android Studio 사용

### 1단계: Android Studio에서 새 프로젝트 만들기

1. **Android Studio 실행**
2. **File → New → New Project**
3. **Empty Activity** 선택
4. **Next** 클릭
5. 설정:
   - Name: `TempAndroid`
   - Package: `com.idolfilterapp`
   - Location: `D:\아이돌\idol-filter-app\TempAndroid`
   - Language: **Kotlin**
   - Minimum SDK: **API 21**
6. **Finish** 클릭

### 2단계: android 폴더 구조 확인

생성된 `TempAndroid` 폴더 안에 `app` 폴더가 있습니다.

### 3단계: android 폴더 수동 생성

`TempAndroid` 폴더의 구조를 참고하여:

1. `D:\아이돌\idol-filter-app` 폴더에 `android` 폴더 생성
2. `TempAndroid`의 다음 파일들을 복사:
   - `build.gradle` (루트)
   - `settings.gradle`
   - `gradle/` 폴더 전체
   - `app/build.gradle`
   - `app/src/main/AndroidManifest.xml`
   - `app/src/main/java/` 폴더 구조

### 4단계: package.json 수정

`app/build.gradle`에서 패키지 이름을 확인하고, 필요시 수정

---

## 🔄 또는: 기존 프로젝트에서 복사

다른 React Native 프로젝트가 있다면:

```bash
# 예시: 다른 프로젝트의 android 폴더 복사
xcopy /E /I /Y "C:\다른프로젝트\android" "D:\아이돌\idol-filter-app\android"
```

---

## 💡 추천 순서

1. **Android Studio로 새 프로젝트 생성** (위 방법)
2. 생성된 구조를 참고하여 필요한 파일만 복사
3. 또는 전체 `android` 폴더 구조를 그대로 사용

---

## ⚠️ 주의사항

- `app/src/main/java/` 경로의 패키지 이름을 프로젝트에 맞게 수정
- `AndroidManifest.xml`의 패키지 이름 확인
- `build.gradle`의 설정 확인

---

## 📝 생성 후 확인

`android` 폴더가 생성되면:

1. Android Studio에서 `D:\아이돌\idol-filter-app\android` 열기
2. Gradle 동기화 완료 대기
3. 에뮬레이터 실행
4. Terminal: `npm start`
5. Run 버튼 클릭

