# TempProject 빌드 오류 해결 방법

## ⚠️ 문제

`TempProject` 폴더의 build 디렉토리를 삭제할 수 없습니다.
- Android Studio나 Gradle 데몬이 파일을 사용 중일 수 있습니다.

## ✅ 해결 방법

### 방법 1: Android Studio에서 TempProject 닫기 (권장)

1. **Android Studio에서**
   - File → Close Project (TempProject 닫기)
   - 또는 File → Open Recent → 다른 프로젝트 열기

2. **올바른 프로젝트 열기**
   - File → Open
   - `D:\GlowCam\idol-filter-app\android` 폴더 선택
   - ⚠️ **TempProject가 아닌 idol-filter-app 프로젝트를 열어야 합니다!**

3. **TempProject 폴더 수동 삭제**
   - 파일 탐색기에서 `D:\GlowCam\idol-filter-app\TempProject` 폴더 삭제
   - 또는 터미널에서:
     ```bash
     cd D:\GlowCam\idol-filter-app
     rmdir /S /Q TempProject
     ```

### 방법 2: Gradle 데몬 종료

Android Studio Terminal에서:
```bash
cd D:\GlowCam\idol-filter-app\android
.\gradlew --stop
```

그 다음 TempProject 폴더 삭제

### 방법 3: 컴퓨터 재시작

가장 확실한 방법이지만 시간이 걸립니다.

---

## 🎯 올바른 프로젝트 열기

**중요:** Android Studio에서 다음 폴더를 열어야 합니다:

```
D:\GlowCam\idol-filter-app\android
```

**❌ 잘못된 경로:**
```
D:\GlowCam\idol-filter-app\TempProject
```

---

## 📝 확인 사항

1. Android Studio에서 어떤 프로젝트가 열려 있는지 확인
2. `D:\GlowCam\idol-filter-app\android` 폴더를 열었는지 확인
3. TempProject는 더 이상 필요 없으므로 삭제해도 됩니다

